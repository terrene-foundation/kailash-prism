# Session Notes — 2026-05-06

## Where we are

`prism-node24-bump` workspace is **closed** — todos in `completed/`, redteam
APPROVE, codify proposal appended (status `pending_review`). Bump shipped
24 days before the 2026-06-02 GHA Node 20 force-cut. No in-flight prism
workstream; working tree clean on `main`.

## Read first

1. `CLAUDE.md` — repo entry point (architecture, packages, rules index)
2. `workspaces/prism-node24-bump/journal/0001-DECISION-bump-19-days-early.md` — why this workstream landed early; institutional pattern captured
3. `workspaces/prism-node24-bump/04-validate/redteam-round-1.md` — APPROVE verdict + 5 mechanical check transcripts (model for future bump verification)
4. `.claude/.proposals/latest.yaml` — 4 entries appended this session (incl. `future-prism-release-workflow-defaults` rule_candidate); `pending_review` until loom Gate-1
5. `workspaces/prism-0.6.0/.session-notes` — three-trap punch list this session closed; pattern reference for future closeout sessions

## In-flight state

- `.claude/.proposals/latest.yaml` is `pending_review` with this session's 4 new entries; loom Gate-1 picks them up on the next `/sync`. No prism-side action until then.
- All three traps from `workspaces/prism-0.6.0/.session-notes` are now closed; that punch list is historical.

## Traps

- **Don't re-open `prism-node24-bump`.** Closed workspaces stay in tree as institutional record (per `prism-0.6.0` precedent); new bumps get new workspaces.
- **Cross-repo work blocked.** arbor wave-6 is YOUR arbor session per `rules/repo-scope-discipline.md`; do not touch arbor source from a prism session.
- **Loom-sync residue may recur.** Next `/sync` from loom may land 50+ untracked `.claude/**` files again — commit promptly per the PR #34 pattern, never `git add -A`.
- **Future Prism release workflows MUST inherit Node 24 + setup-node@v5 + checkout@v5 + matrix `[22, 24]` CI** per the codified rule_candidate. When `release-compiler.yml` ships, cite `workspaces/prism-node24-bump/01-analysis/02-decisions-locked.md` as the canonical reference.
- **Spec edits to `docs/specs/05-*.md`**: the monolith was split into 13 sub-files (PR #35); always edit the relevant `05a..05m` sub-file directly, not the master `05-engine-specifications.md` (now a thin TOC index).
