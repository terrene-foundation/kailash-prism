# Session Notes — 2026-04-29

## Where we are

Prism web 0.5.0 work split into two PR-ready local branches per the
new `rules/git.md` § "Release-Prep PRs MUST Use release/v* Branch
Convention". Both branches are local; remote push pending user
approval. Arbor PR #17 (wave-1/2/3 + lint fix) OPEN at
terrene-foundation/arbor, awaiting admin-merge. Local-only
`feat/prism-0.4.0-upgrade` in arbor is staged on top of #17, awaiting
rebase + push after #17 squash-merges.

## Read first

1. `journal/0025-DECISION-layout-delegation-wrapper-0.5.0.md` — 0.5.0 work + spec-sweep correction (most recent)
2. `journal/0024-DECISION-arbor-wave3-ship-and-0.4.0-upgrade-stage.md` — kitchen-sink-branch escape + 0.4.0 upgrade staging
3. `01-analysis/layout-migration-scoping.md` — drove Option 2 partial-delegation
4. `web/CHANGELOG.md` — 0.5.0 dated 2026-04-28
5. `01-analysis/arbor-wave4-route-selection.md` — `/clients` is wave-4 target after 0.4.0+0.5.0 ship
6. `journal/0023-DECISION-redteam-convergence-round2-clean.md` — prior /redteam baseline

## In-flight state

- Local branches ready (NOT pushed):
  - `feat/prism-web-0.5.0-layout-delegation` — chore (test-setup jsdom fix) + feat (layout delegation code) + docs (workspace journals + this notes file). NO version bump, NO CHANGELOG.
  - `release/v0.5.0` — version bump (web/package.json 0.4.0→0.5.0) + CHANGELOG entry only. Branches off the feat branch's tip so the version bump references the merged feat code.
  - `backup-2026-04-29` — preserves the original 3-commit local-main state in case rollback is needed.
- Arbor PR #17 OPEN. Cron watcher `trig_01BdEn4bUeeXxiy3JW1RkgT6` armed daily 9am SGT — surfaces merge + the local rebase script when state flips to MERGED.
- 3 spec-mandated types (`LayoutEngineConfig`, `ZoneContent`, `ResponsiveValue<T>`) restored to public barrel after spec sweep caught the audit miss — six symbols actually removed, not nine.
- **jsdom navigation warning suppressed at root cause** (test-setup.ts capture-phase preventDefault on `<a href>` clicks). Pre-existed since 0.3.1 (`data-table-card-grid.test.tsx:229` clicks an anchor for stopPropagation assertion); navigation queued via setTimeout fired during the next test as cross-test contamination. Production behavior unchanged. Tests now 403/403 with zero stderr noise.

## Traps

- **`dist/` is stale** (pre-0.5.0 build, no `dist/engines/layout/` directory yet). `/release` rebuilds via `tsc`. Do NOT ship the stale dist or treat its absence of new-engine sub-path as a missing source.
- **`Split` / `Layer` / `Scroll` kept legacy deliberately** in 0.5.0 — semantics (static ratio + mobile collapse / backdrop + focus trap / scrollbar toggle) have no equivalent in `engines/layout/`. Do NOT force-delegate without consumer demand.
- **`resolveBreakpoint` and `ZoneContent`** still module-exported from `engines/layout.tsx` (for `layout.test.tsx` boundary tests) but only the latter is in the public barrel.
- **Arbor `feat/prism-advisory`** holds 12 orphaned maintenance commits with `pyproject.toml`/`uv.lock` drift vs origin/main — separate untangling, do NOT mix into Prism PRs.
- **0.4.0 upgrade branch needs rebase onto squashed-PR-#17 commit** before push. G-1 changes and lint refactor touch disjoint regions of `documents-prism/page.tsx` → conflict-free.
- **Spec sweep before deletion**: `web/src/` audits miss `docs/specs/**` and `specs/**`. A symbol with 0 code consumers but a spec mention is contract-bound, not vestigial.

## Open questions for the human

- Two PRs ready to push for Prism BUILD repo:
  1. `feat/prism-web-0.5.0-layout-delegation` → PR A (code only, ~3 commits)
  2. `release/v0.5.0` → PR B (metadata-only: package.json + CHANGELOG)
  Push order: PR A first (admin-merge), then PR B (admin-merge), then push `web-v0.5.0` tag to trigger `release-web.yml`.
- Admin-merge arbor PR #17 when ready (still MERGEABLE, OPEN). Independent of 0.5.0 release.
- Wave-4 `/clients` cadence after PR #17 + 0.4.0 upgrade ship.
