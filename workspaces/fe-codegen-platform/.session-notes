# Session Notes — 2026-04-20/21 (parallel wave + /redteam convergence)

## Where we are

Prism web engine at 0.4.0 on main. Ten PRs merged this session
(`01b1c1d..ac55dc6`), advancing all four framings F1 (engine
completeness) / F2 (codegen) / F3 (consumer validation — partial) /
F4 (production-grade). /redteam converged after round-1 surfaced
3 HIGH and PR #17 resolved them; round-2 clean.

## Read first

1. `workspaces/fe-codegen-platform/journal/0023-DECISION-redteam-convergence-round2-clean.md`
   — full session arc: what shipped, convergence verdict, outstanding
   work.
2. `workspaces/fe-codegen-platform/journal/0020-RISK-flutter-parity-redteam-round1-findings.md`
   — Flutter parity gap (compiler output dead code, Riverpod absent,
   200-LOC cap violated 5/5). Multi-session workstream.
3. `workspaces/fe-codegen-platform/journal/0021-GAP-layout-engine-orphan-partial-resolution.md`
   — new Layout engine sub-path-gated; legacy→new migration needed.
4. `workspaces/fe-codegen-platform/01-analysis/arbor-wave4-route-selection.md`
   — /clients identified as next arbor migration target (but see
   "Traps" below — arbor does NOT have wave-3 landed yet).
5. `web/CHANGELOG.md` — 0.4.0 + Unreleased (Layout engine sub-path).

## What shipped (PRs in order)

| PR  | Commit    | Scope                                                       |
| --- | --------- | ----------------------------------------------------------- |
| #11 | `a41c9ad` | chore: wave-3 polish + 0.4.0/wave-4/distribution scoping    |
| #12 | `54e3498` | feat(ops): GitHub Release asset distribution workflow       |
| #13 | `6ebc341` | feat(compiler): codegen pipeline PoC                        |
| #14 | `7b5c686` | feat(prism-web): Layout engine (6 primitives, sub-path)     |
| #15 | `56340ba` | test(prism-web): Tier 2 engine wiring tests (3 engines)     |
| #16 | `421dd41` | feat(prism-web)!: 0.4.0 — TId + globalSearch + comparator   |
| #17 | `1c47a20` | fix(prism): /redteam round-1 — 3 HIGH + 1 MED + 1 LOW       |
| #18 | `7daf973` | docs(journal): convergence entries 0021/0022/0023           |
| #19 | `35f455f` | fix(journal): frontmatter + For Discussion                  |
| #20 | `ac55dc6` | chore(specs): version field on 13 atom specs (closes 0022)  |

Test posture on main: 387/387 web + 73/73 compiler = 460/460 passing.
`tsc --noEmit` clean in web/ and compiler/.

## /redteam verdict

Round 1 (4 parallel agents — spec / security / code / tests):
- 0 CRIT / 3 HIGH / 8 MED / 12 LOW

Round 1 HIGH blockers + dispositions (PR #17):
- HIGH-1 Layout orphan → sub-path gate (full migration deferred)
- HIGH-2 Layout* barrel drag → 30-line re-export removed
- HIGH-3 `data-table.yaml` v0.3.0 vs 0.4.0 code → spec bumped + regen

Round 2 (single analyst re-validation):
- 0 new CRIT/HIGH/MED/LOW — all round-1 resolutions AST/grep verified
- Declared converged

## Deferred workstreams (by size)

1. **Layout engine legacy→new unification** — journal 0021. ~2-3
   sessions. Scope: grow `engines/layout/` with orchestration
   (LayoutProvider/useLayout/useResponsive/Zone) OR have legacy
   delegate to new primitives. Then migrate 10 templates. Then
   delete `engines/layout.tsx`.

2. **Flutter parity** — journal 0020. Multi-session. Compiler's
   Flutter output is orphan (`compiler/generated/flutter/*.dart`
   never imported); `flutter/lib/theme/prism_colors.dart` is
   hand-authored and diverges. Riverpod absent. All 5 engines
   violate 200-LOC cap (k_chat.dart is 1,694 LOC). 1 failing test
   (`KConversationSidebar` date-group label) — zero-tolerance carry.

3. **Arbor wave-1/2/3 commitment + wave-4** — prerequisite gap
   surfaced in 2026-04-21 wrap-up. Arbor main at `d1a6c44` has NO
   Prism integration. The wave-1/2/3 work from prior sessions lives
   on local branch `feat/prism-advisory` + one git stash on that
   branch (`arbor-wip-prism-and-sync`). Wave-4 `/clients` cannot
   start until wave-3 is landed on arbor main. Scope: either (a)
   PR feat/prism-advisory into arbor main and ship the stash, or
   (b) re-scope from scratch targeting the current 0.4.0 API.

## Traps (for the next session)

- **Arbor state mismatch.** Previous `.session-notes` said "wave-3
  shipped end-to-end" — that was in the working tree. It was
  stashed, never committed, never PR'd. Arbor main has zero Prism.
  Before promising wave-4, unstash on `feat/prism-advisory`, decide
  to ship or abandon.
- **`engines/layout.tsx` is the canonical Layout import.** New
  `engines/layout/` primitives are at the sub-path only. Do NOT
  import Layout primitives from the top-level barrel — they aren't
  there anymore.
- **Distribution via GitHub Release assets is live but unused.**
  The `.github/workflows/release-web.yml` fires on `web-v*` tags.
  No `web-v0.4.0` tag has been pushed yet. Cut the release tag
  before telling a consumer to use the release asset URL.
- **Spec-loader is strict on `version:`.** All 70 component specs
  have it now. Don't introduce a new atom spec without `version:` —
  batch codegen will refuse with SCHEMA_VIOLATION.
- **`specs-authority.md` Rule 5b** (sibling-spec sweep on edits) is
  now codified — every future spec edit triggers a full sibling
  grep across the same domain.
- **Stale `.spec-coverage-v2.md` Layout claim.** Line 126 still says
  "PASS" on a removed alias. Non-blocking, refresh on next codify.

## Next candidate threads (ranked by leverage)

1. **~~Ship the 0.4.0 GitHub Release~~** — DONE 2026-04-21. `web-v0.4.0`
   tag cut; `.github/workflows/release-web.yml` fired successfully
   (run #24704781006); tarball published at
   `https://github.com/terrene-foundation/kailash-prism/releases/download/web-v0.4.0/kailash-prism-web-0.4.0.tgz`.
   Node 20 deprecation warning noted in workflow annotations
   (action runtime migration to Node 24 required before 2026-09-16) —
   track as a future hardening pass.
2. **Arbor feat/prism-advisory resolution** — decide ship vs abandon.
   If ship: PR into arbor main, then wave-4 `/clients` can start.
3. **Layout migration shard** (journal 0021) — 2-3 sessions.
4. **Flutter parity wave** (journal 0020) — multi-session.

## Memory + env pointers

- `memory/MEMORY.md`: Prism = GitHub-only distribution; squash-merge
  preference; arbor Prism integration pattern.
- `.claude/rules/` full complement loaded; rule 5b (specs) and
  pytest plugin+marker rule (testing) codified this week.
- Deferred items are tracked in journal, not as todos — workspace
  has no active todos as of this session.
