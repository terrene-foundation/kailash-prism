name: kailash_prism
description: "Kailash Prism Flutter engine — composable widgets, design tokens, page templates."
version: 0.1.0
homepage: https://terrene.dev
repository: https://github.com/terrene-foundation/kailash-prism
issue_tracker: https://github.com/terrene-foundation/kailash-prism/issues

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
